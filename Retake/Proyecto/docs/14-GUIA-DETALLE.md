# 🪪 PASO 14 — Página de detalle (`jugador.html?id=3`)

> ⏳ **Pendiente — se escribe al llegar al paso, partiendo del código real.**
>
> **Se construye:** click en un jugador → otra página con su ficha completa (aquí por fin se usa la ruta `GET /:id` de `03-GUIA-MYSQL` sección 6).
> **Se aprende:** navegación entre páginas y `URLSearchParams` — el espejo de `req.query`, del lado del front.
