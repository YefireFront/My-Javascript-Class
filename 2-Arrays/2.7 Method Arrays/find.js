//(*  ENCONTRAR EL PRIMER ELEMENTO QUE CUMPLE CON UNA CONDICION ( find )

// const findneado = greatsTeams.find(country => country.country === 'US')
// console.log(`The team that macth is`);
// console.log( findneado);

let yeffer = "We are  agreatrer programer";
let a = 119;
let b = 115;

let c = a + b;
var f = undefined;
const message = true;

a;

if (a > b) {
  console.log(`${a} es mayor`);

  a = 20;
  if (c % 2 == 0) {
    console.log(`${c}    es par`);
    let b;
    a;
    b = a + 20;
    b;
  }
} else {
  console.log(`${b} es mayor`);
  if (b % 2 !== 0) {
    a = 115;
    console.log(`${b} es impa o something like that`);
  }
}
if (yeffer.lenght) {
  
} else {
  
}